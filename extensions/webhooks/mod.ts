/**
 * Webhook ingress mappers for Hooksmith.
 *
 * Provider families are exposed through focused subpaths such as
 * `@hooksmith/webhooks/sns` so provider-specific dependencies remain isolated.
 *
 * @module
 */

export {};
