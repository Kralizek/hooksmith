# Hooksmith GitHub Action

Reserved for the first-party GitHub Action host for Hooksmith.

The v1 runtime is intentionally CLI-first. The action will wrap the same runtime rather than introduce different execution semantics.
